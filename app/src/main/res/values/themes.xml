<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StudentHub" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrim ary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StudentHub" parent="Base.Theme.StudentHub" />
    <style name="Theme.StudentHub.SplashScreen" parent="Theme.StudentHub">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/iiitl</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.StudentHub</item>
    </style>
</resources>